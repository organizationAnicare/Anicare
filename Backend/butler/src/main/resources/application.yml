spring:
  h2:
    console:
      enabled: true
  profiles:
    active: local
  datasource:
    jdbc-url: jdbc:h2:tcp://localhost/~/test
    driver-class-name: org.h2.Driver
    username: sa
    password:

jpa:
  hibernate:
    ddl-auto: create